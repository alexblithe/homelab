{{- if not .Values.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "searxng.secretName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "searxng.labels" . | nindent 4 }}
type: Opaque
data:
  SEARXNG_SECRET: {{ default (randAlphaNum 64) .Values.searxngSecret | b64enc | quote }}
{{- end }}
